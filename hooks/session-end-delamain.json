{
  "_comment": "Claude Code per-hook config. Keep plugin-rooted script paths in args using ${CLAUDE_PLUGIN_ROOT}; see hooks/CLAUDE.md for the Claude/Codex split.",
  "description": "Kill Delamain dispatchers and clean heartbeat files on real session exit (not clear/resume)",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/delamain-stop.sh"],
            "timeout": 5
          }
        ]
      }
    ]
  }
}
