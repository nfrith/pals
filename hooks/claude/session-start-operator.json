{
  "_comment": "Claude Code per-hook config under hooks/claude/. Keep plugin-rooted script paths in args using ${CLAUDE_PLUGIN_ROOT}; see hooks/CLAUDE.md for the Claude/Codex split.",
  "description": "Inject stable operator profile context into Claude sessions",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/operator-config-session-start.ts"],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
