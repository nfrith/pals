{
  "_comment": "Claude Code per-hook config under hooks/claude/. Keep plugin-rooted script paths in args using ${CLAUDE_PLUGIN_ROOT}; see hooks/CLAUDE.md for the Claude/Codex split.",
  "description": "Final validation gate — blocks Claude from finishing if any ALS system has errors",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/als-stop-gate.ts"],
            "timeout": 30
          }
        ]
      }
    ]
  }
}
