{
  "_comment": "Claude Code per-hook config. Keep plugin-rooted script paths in args using ${CLAUDE_PLUGIN_ROOT}; see hooks/CLAUDE.md for the Claude/Codex split.",
  "description": "Records which ALS systems and modules were touched during this session",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bun",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/als-breadcrumb.ts"],
            "timeout": 5
          }
        ]
      }
    ]
  }
}
