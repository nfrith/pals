# Docs Skill Directory

Every file in `references/` must have a matching entry in this directory's `SKILL.md` index.

When a change adds, removes, or renames a file under `references/`, update `SKILL.md` in the same change so the index stays complete.
