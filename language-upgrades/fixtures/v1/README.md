# ALS v1 Fixture

This frozen fixture is seeded from `reference-system/`.

Included:

- `.als/`
- `clients/`
- `dotfiles/`
- `governance/`
- `infra/`
- `operations/`
- `workspace/`

Excluded:

- `.claude/`
- non-mounted top-level content outside the authored ALS surface

Do not mutate this fixture in place. Add a new retained fixture for later `als_version` releases instead.
