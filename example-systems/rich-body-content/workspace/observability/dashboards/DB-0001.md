---
id: DB-0001
title: API Gateway Latency Watch
stream_ref: "[STR-0001](als://rich-body-content/observability/metric-stream/STR-0001)"
---

# API Gateway Latency Watch

## SUMMARY

Primary latency dashboard for the API gateway. The backing stream is stored as a JSONL entity so ALS can validate row consistency without forcing the data through markdown sections.

## WATCHPOINTS

- Compare `p50`, `p95`, and `p99` across the same observation windows.
- Treat `canary` rows as comparable only against other `canary` rows.
- Keep schema evolution out of the dashboard document and in the entity contract.
