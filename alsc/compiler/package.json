{
  "name": "@als/compiler",
  "version": "0.1.0-beta.1",
  "private": true,
  "description": "ALS compiler — validates ALS systems and projects Claude skill bundles.",
  "license": "Elastic-2.0",
  "type": "module",
  "scripts": {
    "cli": "bun src/cli.ts",
    "validate": "bun src/index.ts",
    "deploy:claude": "bun src/deploy.ts",
    "test": "bun test",
    "test:parallel": "bun test --concurrent --max-concurrency=20",
    "test:stress": "bun test --concurrent --randomize --rerun-each 3"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "mdast-util-from-markdown": "^2.0.2",
    "mdast-util-gfm-table": "^2.0.0",
    "micromark-extension-gfm-table": "^2.1.1",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  }
}
